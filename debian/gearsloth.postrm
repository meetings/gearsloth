#!/bin/sh -e

DIR=/var/lib/gearsloth

case $1 in
  purge)
    rm -f /etc/gearsloth/gearsloth.json
    rmdir --ignore-fail-on-non-empty /etc/gearsloth
    rmdir --ignore-fail-on-non-empty $DIR
    deluser --quiet gearsloth >/dev/null 2>&1 || true
  ;;
esac

exit 0
