#!/bin/sh -e

USERNAME=gearsloth
DIR=/var/lib/gearsloth

if [ "$1" != "configure" ]; then
  exit 0
fi

if ! getent passwd $USERNAME >/dev/null; then
  adduser --system --home $DIR $USERNAME
fi

exit 0
